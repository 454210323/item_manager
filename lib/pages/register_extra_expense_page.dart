import 'package:flutter/material.dart';

class RegisterExtraExpensePage extends StatelessWidget {
  const RegisterExtraExpensePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Register New ExtraExpense')),
      body: const Text("This is a Resister Page"),
    );
  }
}
